server {
    listen 3000;
    root /usr/share/nginx/html;
    index index.html;

    # All routes → SPA (handles /admin/login, /community/dashboard, etc.)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
